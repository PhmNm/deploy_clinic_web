{
    "builds": [{
        "src": "clinic_web/wsgi.py",
        "use": "@vercel/python",
        "config": { "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "clinic_web/wsgi.py"
        }
    ]
}